<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/left_drawer"
    android:layout_width="@dimen/navigation_width"
    android:layout_height="match_parent"
    android:layout_gravity="start"
    android:background="@android:color/darker_gray"
    android:clickable="true"
    >
  <RelativeLayout
      android:id="@+id/user_info"
      android:layout_width="match_parent"
      android:layout_height="200dp"
      android:layout_marginTop="10dp"
      android:background="@color/blue_500"
      >
    <ImageView
        android:id="@+id/avatar_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/marmedsa_logo"
        android:scaleType="centerCrop"
        />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/avatar_bg_color"
        ></FrameLayout>

    <com.mnm.jam.mnmdemo.ui.custom.CircleImageView
        android:id="@+id/avatar"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:layout_centerInParent="true"
        android:src="@drawable/marmedsa_logo"
        />

    <TextView
        android:id="@+id/user_name"
        android:layout_margin="10dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/avatar"
        android:textColor="@color/drawable_bg_color"
        android:text="@string/company"
        android:textSize="@dimen/company_text"
        android:textStyle="bold"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        />
  </RelativeLayout>

  <RelativeLayout
      android:layout_below="@+id/user_info"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:textColor="@color/drawable_bg_color"

      >

    <TextView
        android:layout_margin="35sp"
        android:layout_centerHorizontal="true"
        android:textColor="@color/white_50"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/made_size"
        android:layout_centerInParent="true"
        android:text="@string/demo"
        />
  </RelativeLayout>

</RelativeLayout>